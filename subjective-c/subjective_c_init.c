#include <stdbool.h>

// Temporary placeholder translation unit for the Subjective-C static library.
// Will be replaced once the actual modules (memory, strings, etc.) are migrated.

int subjective_c_placeholder(void) {
    return true ? 0 : 1;
}
