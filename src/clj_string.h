#ifndef CLJ_STRING_H
#define CLJ_STRING_H

// Forward declaration to avoid circular dependencies
struct CljObject;

// === Legacy API (deprecated - use CljValue API) ===
// make_string_old declaration removed - use make_string from value.h instead

#endif