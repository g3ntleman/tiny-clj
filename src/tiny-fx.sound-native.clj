R"TINY_SND_RUNTIME(
(ns tiny-fx.sound-native)
;; Internal native sound namespace.
;; Public callers should prefer tiny-fx.sound.
;; Ramp/bend removed from this ns; use tiny-fx.sound DSL (NOTE_Hz) instead.
)TINY_SND_RUNTIME"
