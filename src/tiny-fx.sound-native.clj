R"TINY_SND_RUNTIME(
(ns tiny-fx.sound-native)
;; Internal native sound namespace.
;; Public callers should prefer tiny-fx.sound for the curated high-level API.
;; Low-level runtime control stays here and is opt-in.
)TINY_SND_RUNTIME"
